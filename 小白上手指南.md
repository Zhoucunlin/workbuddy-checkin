# 🍃 WorkBuddy 每日签到 · 小白上手指南

> 你什么都不用懂，照着下面一步步做就行。✅ = 已经帮你做好了，❌ = 需要你动手。

---

## 你现在的位置

| 步骤 | 状态 | 说明 |
|---|---|---|
| 1. 拿到 Token | ✅ 已完成 | 我已从你电脑的登录态里提取好 |
| 2. 本地签到成功 | ✅ 已完成 | 今天 +100 分已到手（`streak_days=3`） |
| 3. Git 仓库建好 | ✅ 已完成 | 代码已提交，敏感文件已排除 |
| 4. 云端密钥已生成 | ✅ 已完成 | 在 `secret_value.txt` 里 |
| 5. 推送到 GitHub | ❌ 需要你 | 下面第 1 步开始 |
| 6. 配置 Secret | ❌ 需要你 | 复制文件内容粘贴 |
| 7. 开启自动签到 | ❌ 需要你 | 配置好后每天自动跑 |

---

## 第 1 步：创建 GitHub 仓库（约 3 分钟）

1. 打开浏览器，进入 <https://github.com>，登录你的账号（**没有账号先注册一个**，免费）。
2. 点击右上角 **+** → **New repository**（新建仓库）。
3. 填写：
   - **Repository name**：填 `workbuddy-checkin`（随便起，英文即可）
   - 其他选项保持默认，直接点绿色按钮 **Create repository**。
4. 创建成功后页面会显示一个网址，类似：
   ```
   https://github.com/你的用户名/workbuddy-checkin.git
   ```
   **复制这行网址**，下一步要用。

---

## 第 2 步：把代码推上去（约 2 分钟）

1. 打开文件管理器，进入 `E:\WorkBuddy\签到\workbuddy-checkin`。
2. 在文件夹地址栏输入 `cmd` 回车，会弹出黑色命令行窗口。
3. 把下面命令**整行复制**进去（把网址换成你自己的），回车：
   ```
   git remote add origin https://github.com/你的用户名/workbuddy-checkin.git
   git branch -M main
   git push -u origin main
   ```
4. 第一次会弹窗让你登录 GitHub（网页或令牌登录都行），登录后自动推送完成。
5. 看到类似 `main -> main` 就成功了。

> 💡 如果提示让你输入用户名密码，请改用**令牌（Token）方式**：
> GitHub 头像 → Settings → Developer settings → Personal access tokens → Tokens (classic)
> → Generate new token，勾选 `repo`，生成后复制。推送时用户名填你的 GitHub 名，密码粘贴令牌。

---

## 第 3 步：配置密钥（约 2 分钟）

1. 回到 GitHub 仓库页面，点顶部 **Settings**（设置）。
2. 左侧菜单往下找，点 **Secrets and variables** → **Actions**。
3. 点绿色按钮 **New repository secret**（新建密钥）。
4. 填写：
   - **Name**：`WORKBUDDY_ACCOUNTS_B64`（**必须一字不差，全大写**）
   - **Secret**：打开你电脑上的 `E:\WorkBuddy\签到\workbuddy-checkin\secret_value.txt`，
     全选复制里面的内容，粘贴进来（一大串乱码一样的字符，正常）。
5. 点绿色 **Add secret**（添加密钥）。

> 🔒 放心：这串内容只保存在 GitHub 的加密保险柜里，别人看不到、日志里也不显示。
> 里面装的就是你的登录凭证，所以绝不能发给别人。

---

## 第 4 步：手动跑一次试试（约 1 分钟）

1. 回到仓库主页，点顶部 **Actions**（操作）标签。
2. 左侧列表里会看到一个工作流 **"WorkBuddy 每日签到"**，点它。
3. 右侧出现 **Run workflow**（运行工作流）按钮，点它 → 再点绿色 **Run workflow** 确认。
4. 稍等几秒，会出现一次运行记录，点进去看日志。
5. 滚动日志到底部，看到：
   ```
   [我的账号] ✓ 签到成功，获得积分 100
   汇总: 共 1 个账号 | 签到成功 1
   ```
   就说明云端也能正常签到！🎉

> ⚠️ 如果你今天已经在本地签过到了，云端这次会显示「今日已签到」并跳过，
> 这也是正常的，说明脚本正确识别、不会重复扣分。

---

## 第 5 步：开启每日自动签到（零操作）

从第 4 步完成那一刻起，**就全部搞定了**。

GitHub 会在**每天早上 8:00（北京时间）**自动运行这个脚本，
无需你开电脑、无需登录、完全免费。签到结果会记录在每次运行的日志里。

> ⏰ 小知识：GitHub 定时任务有时会延迟 15 分钟～2 小时执行，属于平台机制，
> 不用担心，当天任意时间签一次效果一样。

---

## 以后怎么查看签到结果？

- 打开 GitHub 仓库 → **Actions** → 点击最新一次运行 → 看日志。
- 日志里每行都有时间和结果，`✓` 成功 / `已签到` 跳过 / `✗` 失败。

## 以后怎么换账号 / 加账号？

- 多账号：把 `secret_value.txt` 里的内容换成多个账号的 JSON，重新 base64 编码，
  更新同一个 Secret 即可（具体格式见 README.md）。

## Token 过期了怎么办？

- Token 约 60 天有效。**只要你偶尔打开 WorkBuddy 客户端，它就会自动续期**，无需管。
- 如果 60 多天没开客户端，日志会显示 `401` 失败，此时：
  1. 打开一次 WorkBuddy（刷新登录态）
  2. 告诉我，我帮你重新生成 `secret_value.txt`
  3. 去 GitHub 更新 Secret，搞定。

---

## 常见问题（FAQ）

**Q：推送时报错 "remote: Repository not found"？**
A：仓库网址复制错了，或仓库还没创建。检查第 1 步的网址。

**Q：Actions 显示红色 ✗ 失败？**
A：点进日志看具体原因。90% 是 Token 过期（`401`），按上面"Token 过期"处理。

**Q：日志显示「今日已签到」？**
A：正常！说明今天已经领过了，脚本很聪明，不会重复领也不会扣分。

**Q：GitHub Actions 页面是空的？**
A：需要先把代码推上去（第 2 步），且 `.github/workflows/checkin.yml` 文件必须在仓库里。
